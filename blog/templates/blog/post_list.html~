{% extends 'blog/base.html' %}

{% block content %}
    {% for post in posts %}
        <h1>to see what pk is: {{post.pk}}</h1>
        <div class="post">
            <div class="date">
                {{ post.published_date }}
            </div>
            <h1><a href="{% url 'post_details' pk=post.pk %}">{{ post.title }}</a></h1>
            <p>{{ post.text|linebreaks }}</p>
        </div>
    {% endfor %}
{% endblock content %}
